name: ci

on:
  push:
    branches: [main, master]
    tags: ['v[0-9]*.[0-9]*.[0-9]*']
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          # The minor, at its newest patch, named here rather than read from go.mod: a
          # dependency can drag go.mod's line to an exact patch (sqlite did, to 1.25.0), and
          # setup-go then installs exactly that, which is how govulncheck failed the build on
          # stdlib fixes that shipped in 1.25.2. Keep this in step with go.mod and the
          # Dockerfile's golang:1.27-alpine.
          go-version: '1.27'
          check-latest: true
      - run: go vet ./...
      - run: go test -race ./...
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  image:
    needs: test
    runs-on: ubuntu-latest
    # Every push to the default branch moves :latest; a vX.Y.Z tag also publishes an image
    # with that version, so a self-hoster can pin one and upgrade on purpose. The on.push
    # filters above already restrict pushes to exactly those refs; a tag that is not semver
    # never gets here, because metadata-action would emit no tags for it and the push would
    # fail.
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
      - uses: docker/build-push-action@v7
        with:
          context: .
          # A large share of self-hosters are on ARM; an amd64-only image is a silent bounce.
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
